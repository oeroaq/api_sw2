<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_area :nombre, id: :user_nombre %>
  </div>

  <div class="field">
    <%= form.label :correo %>
    <%= form.text_area :correo, id: :user_correo %>
  </div>

  <div class="field">
    <%= form.label :documento %>
    <%= form.number_field :documento, id: :user_documento %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
